# 从权威空间状态规划玩家移动

自然语言目的地、精确坐标和玩家指定途经点进入同一行动解析，并由本地加权路径规划器从角色当前权威位置计算路线；未指定路线时默认选择符合适用移动、地形、占地和空间规则的最低移动消耗路径。玩家可以用坐标、途经点或自然语言约束覆盖默认路线，存在可感知的借机攻击、危险或掩护变化时必须预览确认，而隐藏实体和未发现危险不得参与优化并会在实际发现时中断移动。该选择使路径计算可复现且不依赖语言模型心算，代价是地图模型必须为每种移动方式维护合法性、成本和角色可知风险。
