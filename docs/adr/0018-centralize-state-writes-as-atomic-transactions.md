# 通过单一接口原子化战役状态写入

dnd-5e-campaign-state 是唯一持久写入边界；其他十项 Skill 只能提交带前置修订、来源、受众和预期差异的状态变更请求。该边界统一校验并以状态事务同时提交事实、认知、资源、时间和事件；实体快照是当前权威状态，事件日志用于审计和恢复。战役级写锁串行化提交，修订冲突拒绝旧请求并触发重新解析，崩溃恢复不得留下半写状态。该选择保证跨 Skill 写入一致且可审计，代价是所有状态变化都依赖共同命令接口。
