# 使用明确的 IC、OOC 与系统消息格式

玩家以 `“……”` 表达角色对话、`*……*` 表达角色行动、`（内心：……）` 表达角色内心、`//` 表达局外交流，`【……】` 仅用于主持 Agent的桌务或系统输出以及人类骰子报告；无标记的玩家普通文本默认作为 OOC。玩家只描述行为，不主动指定检定，主持 Agent决定是否需要规则结算。文本格式只区分语义，发送者与角色控制权仍由稳定名册和交互模式确定。该选择降低 AI 对线上文字语义的误判，代价是玩家需要遵守一套轻量输入约定。
