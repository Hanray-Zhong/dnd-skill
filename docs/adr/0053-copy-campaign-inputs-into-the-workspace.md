# 将战役输入复制进工作区并保持不可变

用户导入的模组、角色资料和附件必须复制到 `inputs/`，计算内容哈希并分配稳定输入 ID，数据库、模组原型和地图模型只能引用该 ID，不能依赖下载目录、网络位置或其他工作区外路径。输入原件在逻辑上不可变，相同内容可按哈希去重，任何变化形成新版本，文件名只作清理后的元数据，脚本、PDF JavaScript、宏、附件和外链永不执行；已被引用的输入只能通过显示影响的管理流程删除。该选择保证战役可移植、可追溯且不受外部文件移动影响，代价是每个工作区需要保存其战役专属输入副本。
