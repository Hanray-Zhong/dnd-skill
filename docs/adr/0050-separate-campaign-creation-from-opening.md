# 严格分离战役创建与打开

创建新战役只接受新建或完全空目录，先完成目录与状态库初始化并最后原子写入 `campaign.json` 作为完成标志，失败时只清理本次创建且身份明确的暂存内容；非空目录不得自动合并、覆盖或清理。打开既有战役必须从合法根清单验证战役身份、schema、兼容组合、共享资产哈希、必要目录和 SQLite 完整性，缺少权威状态时停止，只有投影缺失才自动重建；所有清单路径必须为不能通过符号链接或上级引用逃逸工作区的相对路径。该选择防止误覆盖、目录猜测和静默升级，代价是 create、open、repair 与 migrate 必须成为明确不同的工作流。
