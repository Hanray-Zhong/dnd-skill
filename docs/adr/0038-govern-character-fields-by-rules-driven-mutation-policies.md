# 以规则驱动的权限变更角色字段

角色数据的每个字段都必须按适用规则声明字段变更权限，包括变更主体、开放触发、合法值、资源与时间成本、提交锁定点及下次开放条件；公式派生字段只重算，事件驱动字段只随已结算事件变化，玩家选择字段只在角色选择窗口内接受提交。合法选择一经状态事务提交即保持锁定，玩家角色卡上的不同值只能发起对账而不能直接覆盖，例如准备施法者在规则允许的长休后提交并锁定准备法术，而已知施法者只在相应职业升级等规则触发时替换法术。该选择忠实表达不同职业、特性、装备、状态和模组覆盖的修改时机，代价是规则构建必须为每个可变字段生成并验证相应权限目录。
