# 根清单只保存启动与兼容性信息

`campaign.json` 只保存固定格式标识与主版本、不可变 campaign_id、创建时间、SQLite 引擎及相对路径、Skill Suite、规则章节库、公式目录、状态 schema 与地图 schema 的版本和哈希，以及不能覆盖标准字段的具名扩展。当前修订、玩家名册、模组实例、会话、秘密和其他游戏状态均只存在战役状态库，当前战役名称也不进入根清单；工作区目录可以自由移动或改名而不改变身份。该选择避免根文件成为第二份状态来源并允许打开数据库前完成兼容性判断，代价是任何兼容组合变化都必须通过显式迁移安全更新根清单。
