# 将可中断行动结算为因果事务链

移动及其他可能触发借机攻击、陷阱、预备动作或反应的行动使用一个稳定 action_id，但只推进到下一个规则触发点，并把每个已完成片段作为独立原子状态事务按因果序号提交。触发效果可以终止或重算剩余路径，新事实允许玩家调整尚未执行的部分但不能撤回已发生片段；待处理触发点必须持久化，使恢复不会重复反应、骰点或伤害。该选择忠实表达规则中断并避免跨玩家等待时长期占用数据库事务，代价是战斗状态必须显式维护行动链、待处理触发和幂等恢复。
