# 在精确战斗前编译并验证地图

模组导入或冒险准备阶段允许语言模型结合地图、区域键和正文提出带来源与置信度的候选静态地图模型，但确定性代码必须校验坐标、比例、拓扑、占地、区域引用和 schema，缺项或矛盾经人工复核后才能冻结版本与哈希。精确战斗运行时只对已验证模型执行路径、距离、视线、掩护、范围、碰撞、可见性和 SVG 渲染，不再调用大模型识图、补充几何或生成图片；没有已验证地图时只能在请求先攻前编译并核对一次结构化示意地图，否则降级为区域或剧场式模式。该选择把高成本且可能不确定的语义理解移到准备阶段，代价是任何可能进入精确战斗的地点都需要提前通过地图完整性门禁。
