# 显式且原子地迁移战役版本

运行中的战役固定其规则章节库、公式目录、模组实例和状态结构组成的战役兼容组合，Skill 更新不得静默替换。升级必须先执行只读迁移预演并展示字段、规则语义、冲突及数据损失风险，再创建迁移前快照，通过版本化迁移和统一状态事务原子提交；失败时完整回滚。歧义、破坏性或不可逆变化需要人工确认，未确认则继续使用旧组合。成功后保留迁移记录与旧快照，恢复时从快照建立新的状态修订而不改写事件历史。该选择保护长期战役的一致性与可恢复性，代价是需要维护兼容矩阵、迁移程序和旧版本读取能力。
