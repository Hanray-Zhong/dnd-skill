# 通过版本化公式目录执行计算

三宝书明确规定的确定性规则计算必须由本地计算器依据版本化公式目录或查表规则执行，并优先覆盖角色派生数据与即时判定；主持 Agent只负责选择适用项、解释结果并提交状态事务。特性、装备、状态、可选规则和模组覆盖以具名修正项组合，公式缺失、输入不全或冲突时停止计算并进入待复核。是否检定、如何设定无明确来源的 DC、NPC 动机和叙事后果仍属于主持判断。该选择使计算可重复且可测试，代价是三宝书中每项机械运算与叠加顺序都必须显式编码和维护。
